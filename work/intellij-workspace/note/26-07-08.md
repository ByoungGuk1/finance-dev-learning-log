# springBoot 기본 사용 필터

## requestContextFilter

요청하면 현재 Thread에 요청이 바인딩 된다. `HttpServletRequest`

## formContentFilter

```html
<form method="put"></form>
```

=>  
폼의 contants를 사용 가능하도록 변경

## characterEncodingFilter

post방식에 있어서 한글 깨짐과 같은 캐릭터 인코딩을 사용 가능하도록 변환  
`request.setCharactorEncoding("UTF-8");`

# filter 구현 방법

## 인터페이스 구현

`filter` 인터페이스를 구현 (자카르타 서블릿)

```java
//@Component => config로 설정
public class EncodingFilter implements Filter {
    @Override
    public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain) throws IOException, ServletException {
      // ...
    }
}
```

## 클래스를 상속

`OncePerRequestFilter` 클래스를 상속

```java
//@Component => config로 설정
public class RequestLoggingFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain) throws ServletException, IOException {
      // ...
    }
```

## config

```java
@Configuration
public class FilterConfig {
    @Bean
    public FilterRegistrationBean<EncodingFilter> encodingFilter() {
        FilterRegistrationBean<EncodingFilter> bean = new FilterRegistrationBean<>();
        bean.setFilter(new EncodingFilter());
        bean.setOrder(2);
        bean.addUrlPatterns("/auth/*");
        return bean;
    }

    @Bean
    public FilterRegistrationBean<RequestLoggingFilter> loggingFilter() {
        FilterRegistrationBean<RequestLoggingFilter> bean = new FilterRegistrationBean<>();
        bean.setFilter(new RequestLoggingFilter());
        bean.setOrder(1);
        bean.addUrlPatterns("/*");
        return bean;
    }
}
```

# Interseptor

## 인터페이스 구현

`HandlerInterceptor` 인터페이스 구현

```java
@Component
public class LoginCheckInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, @Nonnull HttpServletResponse response, @Nonnull Object handler) throws Exception {
      // ...
    }
}
```

## config

```java
@Configuration
@RequiredArgsConstructor
public class WebConfig implements WebMvcConfigurer {
    private final LoginCheckInterceptor loginCheckInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(loginCheckInterceptor)
                .addPathPatterns("/**")     // 모든 경로에 적용
                .excludePathPatterns(       // 제외 경로
                        "/auth/login",
                        "/auth/logout",
                        "/css/**",
                        "/js/**",
                        "/images/**",
                        "/auth/signup",
                        "/favicon.ico",
                        "/.well-known/",
                        "/error"
                );
    }
}
```

# 요청별 흐름

```text
HTTP 요청
  ↓
[ Filter 체인 ]  ← Spring Context 밖 (서블릿 영역)
  ↓
DispatcherServlet
  ↓
[ HandlerInterceptor.preHandle() ]  ← Spring MVC 영역
  ↓
[ AOP @Before ]  ← Spring 프록시
  ↓
Controller 메서드 실행
  ↓
[ AOP @After / @Around ]
  ↓
[ HandlerInterceptor.postHandle() ]
  ↓
View 렌더링
  ↓
[ HandlerInterceptor.afterCompletion() ]
  ↓
[ Filter 체인 후처리 ]
  ↓
HTTP 응답
```
