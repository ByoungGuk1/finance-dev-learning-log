26-05-21

---

클래스 생성시마다 자동으로 주석 설정

window > preferences > java > code templates > types(클래스) >

---

클래스 실행 순서

static 변수
static 블럭
static 메서드

instance 변수
instance 블럭
instance 메서드
생성자

---

맨 위에 패키지 경로 지정 안하면 src바로 밑에 default package로 설정

---

패키지 이름은 모두 소문자로 작성. 패키지 이름이 서로 중복되지 않도록 회사 도메인 이름의 역순으로 작성하고, 마지막에는 프로젝트 이름을 붙여줌

---

서블릿 - 싱글톤
서블릿 이전엔 CGI -> 인원수가 많이 들어오면 서버 다운
보완을 위해 서블릿

싱글톤 패턴은 기본 생성자 만으로 충분하다. 여러개가 생길 수는 있다.
오버로딩해서 추가로 생성자를 만들 필요 없이
해당 객체에 바로 접근해서 수정 삭제가 가능하기 때문이다.

---

modifier만 다르고 다른 구성이 동일하다면 오버로딩되지 않는다.
